# Analysis snapshots

`model_choice_audit/` preserves the direct saved-score audit performed before the calibrated-physics extension. Its files and internal checksum inventory are unchanged. Some narrative statements describe measurements that were still missing at that point. The later calibration study supplies physical validation scores and matched CPU execution times; see `results/calibration/reports/` and `docs/CALIBRATED_MODEL_CHOICE.md`.
